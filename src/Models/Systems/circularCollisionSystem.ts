import {
    type GameState,
} from "../GameObjects.ts";

import {Normalize, type Vector2, VectorMagnitude} from "../Vector2.ts";

export function checkForAnyCollision(state: GameState, deltaTime: number){
    
}